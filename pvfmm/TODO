Vector -> std::vector
 - remove global variables in fmm_tree
 - dev_buffer
 - rm ReInit
 - Resize -> resize
 - Dim -> size
 - Vector -> std::vector
Matrix -> std::vector(std::vector) for non-Real_t
debug double Setup phase
vec.h <- kernel, fmm_tree
template -> Real_t
size_t -> int
Remove unnecessary "{}"
"){" -> ") {", "}else{" -> "} else {"
