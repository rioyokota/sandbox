Vector -> std::vector
 - rm dev_buffer
 - rm own_data
 - rm ReInit
 - Resize -> resize
 - Dim -> size
Matrix -> std::vector(std::vector) for non-Real_t
debug double Setup phase
vec.h <- kernel, fmm_tree
template -> Real_t
size_t -> int
Remove unnecessary "{}"
"){" -> ") {", "}else{" -> "} else {"
