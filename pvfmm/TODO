Vector -> std::vector
 - rm ReInit
 - Resize -> resize
 - Dim -> size
 - Vector -> std::vector
Matrix -> std::vector(std::vector) for non-Real_t
debug double Setup phase
vec.h <- kernel, fmm_tree
template -> Real_t
size_t -> int
Remove unnecessary "{}"
"){" -> ") {", "}else{" -> "} else {"
