#ifndef types_h
#define types_h

typedef double real;
const int PP = 6;
#define for_3 for (int d=0; d<3; d++)
#define for_4 for (int d=0; d<4; d++)
#define for_m for (int m=0; m<PP; m++)
#define for_l for (int l=0; l<PP+1; l++)
#define MAX(a,b) (((a) > (b)) ? (a) : (b))
#define MIN(a,b) (((a) < (b)) ? (a) : (b))

#endif
