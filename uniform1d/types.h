#ifndef types_h
#define types_h

typedef double real;
const int PP = 7;
#define for_m for (int m=0; m<PP; m++)
#define MAX(a,b) (((a) > (b)) ? (a) : (b))
#define MIN(a,b) (((a) < (b)) ? (a) : (b))

#endif
