#ifndef types_h
#define types_h

typedef float real;
const int PP = 7;
#define for_2 for (int d=0; d<2; d++)
#define MAX(a,b) (((a) > (b)) ? (a) : (b))
#define MIN(a,b) (((a) < (b)) ? (a) : (b))

#endif
